/** The result: probability, cohort band, predicted peak, curve.
 *
 * Use `describeRisk(flag, meta)` for the wording rather than composing your own
 * sentence — the phrasing is a product rule, not a style choice.
 *
 * `confidence.band` must change how this LOOKS, not just add a label.
 * `bandWeight(band)` gives you label and hint text. */
export default function RiskCard() {
  return <div>TODO: RiskCard</div>;
}
