/** Plot `meta.performance.learning_curve`.
 *
 * Seven points, measured leave-one-subject-out on held-out people: MAE 28.41 at
 * zero logged meals falling to 23.68 by fifteen, most of the gain by the fifth.
 *
 * Read it from the API, do not hardcode it. No chart library is installed — add
 * one or hand-roll the SVG. */
export default function LearningCurve() {
  return <div>TODO: LearningCurve</div>;
}
