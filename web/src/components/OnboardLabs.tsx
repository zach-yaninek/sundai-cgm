/** Lab-panel form + consent gate.
 *
 * Build the fields from `getFields()` and order them by `importance_rank` — every
 * field is optional, so someone who fills only the first three still gets most of
 * the available accuracy. Show which values will be imputed and that filling more
 * narrows the estimate.
 *
 * Gate on `meta.disclaimer` (must_accept) and `meta.exclusions`. Selecting an
 * exclusion means no reading — not a reading with a warning. */
export default function OnboardLabs() {
  return <div>TODO: OnboardLabs</div>;
}
