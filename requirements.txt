# pandas 2.2 is a hard floor: groupby.apply's include_groups argument, used in
# the test suite, does not exist before 2.2.
pandas>=2.2
pyarrow>=14.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0

# rung 3 only (CLIP embeddings). Not needed for the loader, targets or rung 1.
# open_clip_torch>=2.24
# torch>=2.0
